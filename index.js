/**
 * @module
 *
*/

"use strict";

const log = console.log.bind(console);

log("jsonparser is a command line application and can only be run from the command line. Please see 'jsonparser -h' in your terminal.");
